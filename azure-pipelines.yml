# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: ubuntu-latest

steps:
- task: DockerCompose@0
  inputs:
    containerregistrytype: 'Azure Container Registry'
    azureSubscription: 'Azure subscription 1 (965150e6-fb45-4c50-9947-6d4b23bd3177)'
    azureContainerRegistry: '{"loginServer":"petsusbackend.azurecr.io", "id" : "/subscriptions/965150e6-fb45-4c50-9947-6d4b23bd3177/resourceGroups/Backend/providers/Microsoft.ContainerRegistry/registries/petsusbackend"}'
    dockerComposeFile: '**/docker-compose-azure.yml'
    action: 'Run a Docker Compose command'